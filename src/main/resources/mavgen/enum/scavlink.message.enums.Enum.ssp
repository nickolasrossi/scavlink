<% import scavlink.sbt.mavgen._ %>
<%@ val enum: Enum %>
<%@ val pkg: String %>
// Code generated by sbt-mavgen. Manual edits will be overwritten
package ${pkg}

#if (enum.description != "")/**
 * ${enum.description}
 */
#end
object ${enum.name} extends Enumeration #if (enum.isFlag)with Flag #end{
#for (v <- enum.values)
#if (v.description != "")  /**
   * ${v.description}
   */
#end
  val ${v.name} = Value(${v.value})
#end
}
