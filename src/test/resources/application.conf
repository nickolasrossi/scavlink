# akka {
#  loglevel = "DEBUG"
#  actor {
#    debug {
#      receive = on
#      autoreceive = on
#      lifecycle = on
#    }
#  }
#}

scavlink {
  vehicle {
    autoload-parameters = false
    autoload-mission = false
  }

  log {
    enabled = false
  }

  bridge {
    enabled = false
  }
}
